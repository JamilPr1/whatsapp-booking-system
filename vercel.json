{
  "version": 2,
  "buildCommand": "npm install && cd admin-panel && npm install && npm run build",
  "outputDirectory": "admin-panel/dist",
  "installCommand": "npm install && cd admin-panel && npm install",
  "framework": null,
  "functions": {
    "api/[...path].js": {
      "includeFiles": "backend/**",
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[...path].js" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
